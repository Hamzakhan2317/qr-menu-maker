import VenueInformation from "@/components/Settings/VenueInformation";
import { Box } from "@mui/material";
import React from "react";

const page = () => {
  return (
    <Box>
      <VenueInformation />
    </Box>
  );
};

export default page;
